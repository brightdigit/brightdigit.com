# Agent Notes — Corrections & Standing Directives

This file is the running log of Leo's corrections and standing **always/never** directives
for this repo. It is the source of truth for *how* to work here.

**Read this file at the start of every work session, before doing any work.**

**Append one line per directive, proactively (without being asked), whenever Leo makes a
correction or gives an always/never instruction.** Newest lines go at the bottom. Keep each
entry to a single line; if a directive supersedes an earlier one, update or remove the stale line
rather than leaving both.

---

<!-- Append directives below, one per line. Example:
- Always run ./Scripts/lint.sh before committing; never call swiftlint directly.
-->
- Phase 5: `phase-05` is the integration BASE branch — all Phase 5 worktrees branch from it and all Phase 5 PRs target it; never commit directly on `phase-05`.
- TailwindKit (#69) targets Tailwind v4 ONLY; the v2→v4 site migration is a separate worktree done before #67 so #67 stays a byte-identical structural refactor.
- Newsletter archive fixes go to Buttondown ONLY via the #127 one-shot (source = Mailchimp through the rebuilt Spinetail, NOT deprecated ContributeMailchimp); do not rewrite local Content/newsletters/*.md.
- Phase 5 source of truth: use `AGENTS.md` plus this corrections log; ignore stale GitHub issue/milestone/PRD references to Swift 6.3 because the active toolchain is 6.4.
- Phase 5 PRs: worktree agents may push their branch and open a PR with base `phase-05`, but NEVER merge — Leo reviews, approves, and merges every PR himself.
- Subrepos: pull/push everything with `git subrepo pull --all` / `git subrepo push --all` (tree must be clean). `--all` aborts on the first failure — the usual cause is a stale `.gitrepo` parent after a squash-merge/rebase; recover by running `./fix-subrepo-parents.sh` (resets all parents to HEAD, commits), then re-run.
- Testing: ALWAYS use Swift Testing (`import Testing` / `@Suite` / `@Test` / `#expect`), NEVER XCTest, for all NEW tests, repo-wide. Model on `Packages/BrightDigit/ButtondownKit/Tests` house style. Exception: do NOT bulk-convert existing vendored XCTest suites (e.g. the `Packages/Publish/*` stack) — leave them on XCTest.
- Swift settings: vendored Publish-stack packages and TailwindKit use plain standard Swift 6.4 (`// swift-tools-version:6.4` only) — do NOT add the `swift6Features` upcoming-feature `swiftSettings` array (ExistentialAny/InternalImportsByDefault/etc.) or a `Package@swift-6.0.swift` dual manifest to them. (First-party BrightDigit packages like SyndiKit still use the upcoming-feature array.)
- Use `.claude/agent-notes.md` as this repo's corrections log.
- Never pass Markdown-native Buttondown email bodies through the HTML-to-Markdown converter; copy their Markdown directly, while retaining HTML conversion for Mailchimp imports.
- Lint (SwiftLint) rules: ONE top-level type per file (`one_declaration_per_file`), filename must match its type (`file_name`), members ordered subtype→type_property→type_method→instance_method (`type_contents_order`), file ≤300 lines / type body ≤125 (STRICT promotes the warning to an error). So splitting a big file to fix `file_length` needs each new file to be one correctly-ordered type — reorder members and give tests their own per-type files + a shared fixtures type.
- Reproduce CI lint locally with `LINT_MODE=STRICT CI=1 ./Scripts/lint.sh` (per package too); `swift-format` and SwiftLint can disagree on borderline multiline calls — put the closing `)` on its own line to satisfy both, and shorten name aliases so calls fit ≤90 cols.
- Grove gotcha: `grove add <name>` from inside the `phase-05` worktree still branches the new worktree from `main`, NOT `phase-05`. After `grove add`, verify the base and `git reset --hard origin/phase-05` (safe while the branch has 0 unique commits) before starting work.
- TailwindKit: only model officially DOCUMENTED Tailwind v4 utility classes — nothing custom/non-documented (no `btn`, `bg-bellow-*`, typos, or v2/v3-only names). Excluded tokens stay as raw CSS / `.class("…")`.
- #67 Node→Component migration is verified by SEMANTIC (whitespace-normalized) equivalence, NOT strict byte-identical: Plot renders an embedded `.component(...)` via a sub-renderer spliced in as raw text, so nested closing tags lose the parent's 2-space indentation. Output stays structurally identical (same tags/attrs/text/order); only pretty-print whitespace of component subtrees changes. Verify with `diff <(tr -d '[:space:]' <baseline) <(tr -d '[:space:]' <new)` per file (excluding timestamped *.rss AND sitemap.xml, which carry build dates).
- #67 TailwindKit documented-only exclusions (verified against tailwindcss.com/docs v4, 2026-07-16): tokens from the 391 `@apply` directives that are NOT modeled because they aren't documented v4 utilities — `btn`/`btn-normal` (project classes); all `bg-/border-/text-bellow-*` (custom color); `w-5/12` (width fractions documented only for denominators 2–6, twelfths are grid-only); `leading-{tight,snug,normal,relaxed,loose}` (v4 dropped named line-heights — only `leading-none` + numeric `leading-<n>` remain); bare `filter`/`backdrop-filter` (v4 removed the enabling classes, filters auto-compose). Also: v4 border-radius rename — `rounded-xs` is the new smallest (was `rounded-sm`), bare `rounded`→`rounded-sm`. These excluded tokens stay as raw CSS / `.class("…")`.
- Lint coverage gotcha: the per-package `./Scripts/lint.sh` (e.g. under `Packages/**`) does NOT lint the main app `Sources/BrightDigitSite/**` — that is covered only by the repo-root `./Scripts/lint.sh` and the CI `lint` job. Always run the repo-root `CI=1 LINT_MODE=STRICT ./Scripts/lint.sh` before pushing changes to `Sources/`. Also: running the repo-root `./Scripts/lint.sh` WITHOUT `CI=1` invokes `header.sh`, which rewrites/adds file headers across the tree — in the vendored Publish stack that means adding the BrightDigit MIT header on top of the John Sundell header for ALL files, a huge unwanted diff. Use `CI=1 LINT_MODE=STRICT ./Scripts/lint.sh` (non-mutating) to check, and if headers get polluted, `git checkout` the unintended files.
- swift-format `[LineLength]` (limit 90) flags a long `Text("…")`/`Image(url:…)` call even when SwiftLint `line_length` is disabled via `// swiftlint:disable:next line_length`; those are separate linters. Fix by putting the long string/args on their OWN line inside the call: `Text(\n  // swiftlint:disable:next line_length\n  "…"\n)`. swift-format does not flag a bare string-literal-only line.
- #67 component-fidelity gotchas: (1) `Image(url:).class(x)` emits `src` BEFORE `class`, but the original `.img(.class,.src)` nodes usually put class first — use a raw `Node.img(.class,.src)` for any classed image. (2) `.attribute(named:).class()` DOES respect call order (match the original). (3) `Link(url:)` needs a `URLRepresentable` — pass `path.absoluteString` for a Publish `Path`. (4) A bare `<a>text</a>` (no href) needs a raw `Node.a`, not `Link(_,url:"")`. (5) Keep bespoke leaves (iframes with odd attrs, `.data(named:)`, `.raw(...)`) as raw nodes embedded in the component (Node: Component).
- Xcode/SwiftPM build (`swift build` via the Xcode toolchain) requires UNIQUE basenames across a whole target — two files named `PageHeader.swift` in different folders (`Components/About/` vs `Components/Contact/`) fail with `filename "X.swift" used twice`. When folder-organizing namespaced components, name files `Namespace+Type.swift` (e.g. `About+PageHeader.swift`) — they're `extension Namespace { struct Type }` anyway, so it's idiomatic and collision-free. `header.sh` auto-rewrites the header first line to match the current basename, so renames need no manual header edit.
- Deep component nesting can push string/`Text(...)` lines past swift-format's 90-col limit and trigger SwiftLint `multiline_function_chains` on `Type(multiline-args).convertToNode()`. Fix the chain by putting `.convertToNode()` on its OWN line after the closing `)` (both swift-format and SwiftLint accept this; swift-format won't reattach it). Trailing-closure calls like `Header { }.convertToNode()` are exempt from the rule.
- TailwindKit value tokens (#157): extensible `@theme`-scale families (Color, Spacing, Size, MaxWidth, TextSize, FontWeight, Radius, Shadow, DropShadow, Tracking, Ease) are modeled as SwiftUI-`Style`-shaped `protocol Foo: TailwindToken` + `public struct DefaultFoo` (INTERNAL init — public type is required for the `extension Foo where Self == DefaultFoo { static var … }` leading-dot trick, but it's not constructible by name). Fluent methods take `some Foo`; literal families (Spacing/Size) take concrete `DefaultSpacing`/`DefaultSize` so `.gap(4)` works. Fixed CSS-keyword families (Shade, Position, Flex, FlexDirection, ListStyle, Align, Justify, TextAlign, VerticalAlign, ObjectFit, BorderSide) stay CLOSED enums — "custom value" is meaningless. Shade is NOT extensible (v4 `blue-500` = one `--color-blue-500` var; custom = a new Color). Rationale + per-family v4-doc citations: `Packages/BrightDigit/TailwindKit/docs/tailwind-v4-value-model.md`. Removed `TailwindStyle+Custom.swift`; arbitrary values via each family's `.arbitrary(_:)` static, a custom conformer, or `TailwindStyle+Arbitrary.swift` (`.arbitrary(prefix:value:)`, `.arbitrary(prefix:variable:)`, `.custom(property:value:)`).
- Plot component code shares one source of truth for repeated lists: model the items as a data type (a `struct SocialPlatform: CaseIterable` — a struct CAN satisfy CaseIterable by supplying `allCases` manually) exposed via a Plot `EnvironmentKey` (default = the list), and render with `List(items) { … }.listStyle(.ordered).class("…")` instead of hardcoded siblings. `FooterSocialLinks` is the source of truth for social links; the Contact `#social-media` list is the `contactText != nil` subset, filtered IN the loop with `if let` wrapped in `ComponentGroup { }` (List's content closure is `(Element)->Component`, NOT @ComponentBuilder, so a bare no-else `if let` isn't a valid return; ComponentGroup's init IS @ComponentBuilder and buildOptional renders the empty case as nothing).
- Plot node shorthand migration: rewrite `Node<HTML.XxxContext>.member(` → `Node.member(` (keep `Node.`, drop the generic clause) — works for every member EXCEPT `.img` (can't infer Context; keep explicit `Node<HTML.BodyContext>.img`). Bare `.member` (no `Node.`) never works in `Component`-typed positions. Never touch `Node<HTML.…>` type annotations on stored properties/params. Regex: `s/Node<HTML\.[A-Za-z]+>(\.[a-z])/Node$1/g`.
- Contact-page #social-media section is present in HTML but does NOT render visually on the live site — `.social` CSS is scoped to `body > footer > footer > ol.social` only (Styling/styles/styles.css), and styles are injected by main.js (webpack style-loader, no <link>). Separate follow-up: broaden that selector. The 2026-07-16 refactor fixed the 6 dead `/` contact links + `flaticon-newletter` typo (Contact now inherits footer's real URLs + `newsletter` spelling); the CSS render fix is still pending.
- TailwindKit Default token constants: keep public constrained `static var` leading-dot mirrors in protocol extensions; backing `*Value` `static let`s should be `fileprivate` and mirror vars should return those cached values rather than rebuilding with `.init(...)`.
- TailwindKit rename (2026-07-17, Leo): seam protocol `TailwindStyleProtocol`→`TailwindStyle`; concrete builder struct `TailwindStyle`→`TailwindStyleBuilder` (TW alias unchanged).
- TailwindKit seam types un-nested (2026-07-17, Leo — supersedes #44's "Seam nested types now `TailwindStyleBuilder.TailwindClass`/`.Variant`"): `Variant`/`DefaultVariant` and `TailwindClass`/`DefaultTailwindClass` are now TOP-LEVEL bare-named (one per file, like the token types) — reference them as `Variant`/`TailwindClass`, NOT `TailwindStyleBuilder.Variant`.
- TailwindKit token `static let` (2026-07-17, Leo — supersedes the earlier `*Value`/fileprivate note above): each built-in token value is now a `public static let` ON the concrete `Default…` type (e.g. `DefaultColor.slate`, `DefaultVariant.md`); the `where Self == Default…` leading-dot member stays a computed `static var` forwarder `{ .slate }` (a stored `static let` can't live in a protocol extension). Short-named concretes (`xs`/`sm`/`md`/`lg`/`xl`/`px`) need their own `// swiftlint:disable identifier_name` span.
- NEVER convert the leading-dot BUILDER mirrors (`static var flex: TailwindStyleBuilder { TailwindStyleBuilder().flex }`) to `static let`: causes a `circular reference` compile error, and the type-annotation workaround trips SwiftLint `redundant_type_annotation`. They stay computed `static var`.
- ALWAYS give new local packages under `Packages/BrightDigit/**` the full BrightDigit scaffolding that the subrepos carry (2026-07-17, Leo): `Scripts/{lint.sh,header.sh}`, `.swift-format`, `.swiftlint.yml`, `.swift-version`, `.periphery.yml`, `.spi.yml`, `.gitignore`, `LICENSE`, `README.md`, `CLAUDE.md`, `.github/workflows/*`, `.mise.toml`, `.devcontainer/`. The Packages CI `lint` job runs `./Scripts/lint.sh` per package, so a scaffolding-less package (only Package.swift/Sources/Tests) fails lint. Template the lint tooling + workflows from `Packages/BrightDigit/Contribute`.
- Source `.mise.toml` and `.devcontainer/` for new local packages from the REPO ROOT, not from a sibling subrepo (2026-07-17, Leo — "we are using mise and a devcontainer"): root devcontainer uses this repo's image `brightdigit/publish-xml:6.4` (Contribute's copy uses generic `swiftlang/swift:nightly-6.4.x-noble`). For pure library packages drop root's `node`/`[env] NPM_PATH` from `.mise.toml` (no npm/site-build step); keep the Swift tool pins.
- Every package listed in `.github/packages.json` runs `swift test`; a package with no test target fails CI with `error: no tests found`. Give it at least a placeholder Swift Testing target (`@Suite`/`@Test`/`#expect`) matching the ContributeButtondown style.
- For split-off packages whose sources aren't yet documented, relax the two public-docs rules PER PACKAGE rather than mass-documenting: comment out `missing_docs` in that package's `.swiftlint.yml` and set `"AllPublicDeclarationsHaveDocumentation": false` in its `.swift-format`. Ported/generated sources using file-level `// swiftlint:disable all` also need `blanket_disable_command` in that package's `disabled_rules`.
- Platform CI compile failures: prefer `#if canImport(...)`; fall back to `#if os(...)`. Do not default to ENABLE_WATCHOS=false or raising platform floors unless Leo directs. Autofix build/lint; wait for guidance on infra/other non-code failures.
- Availability regressions need a cross-platform BUILD, not just `swift test` on macOS: a macOS-only-available API (e.g. `FileManager.homeDirectoryForCurrentUser`, unavailable on iOS/tvOS/watchOS) passes local macOS tests but breaks the Apple-platform CI legs. Before pushing changes that touch Foundation/system APIs, run `xcodebuild -scheme <X> -sdk iphonesimulator -destination 'generic/platform=iOS Simulator'` (and equivalents) locally. Use `NSHomeDirectory()` (all Apple platforms + Windows-aware) instead of `homeDirectoryForCurrentUser` for `~`.
- Public subrepos run macOS CI on the GitHub-HOSTED runner label `xcode-27` (macOS 26.5, default Xcode 27.0 beta at `/Applications/Xcode_27.0.app`, Swift 6.4-dev, iOS/tvOS/watchOS/visionOS 27.0 SDKs) — NOT `[self-hosted, macOS]` and NOT `macos-26`. Hosted sim legs need `download-platform: true` (runtimes not preinstalled). This is why hosted macOS can build the nightly-6.4 packages without a swiftly snapshot install.
- For package lint CI, follow MistKit's workflow pattern at commit `16b0d830ae4ecf4d7855fe171bb6e19aee21770e` rather than using `jdx/mise-action` directly.
- MistKit is public; when its workflow is referenced, fetch it directly from `raw.githubusercontent.com/brightdigit/MistKit/refs/heads/main/...` before trying authenticated API, browser, or clone access.
- For this all-subrepo CI repair, run the approved plan unattended through CI verification and PR creation; do not pause for routine status or confirmation when safe in-scope work remains.
- Use SSH remotes for git/subrepo network operations in this repository; do not rewrite them to HTTPS.
- PR #160 is merged; commit the requested subrepo platform-support inventory directly on `phase-05` with `[skip ci]`.
